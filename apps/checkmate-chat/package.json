{
  "name": "@checkmate-assistant/chat",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "description": "Customer-facing one-page chatbot backed by CheckMate reports and the Auth0 MCP server",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prestart": "npm --prefix ../.. run build:core && npm --prefix ../.. run build:app && npm run build",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@auth0/auth0-mcp-server": "^0.1.0-beta.16",
    "@checkmate-assistant/core": "0.1.0",
    "@google/genai": "^2.16.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^17.0.0",
    "openai": "^5.0.0",
    "zod": "^3.25.0",
    "zod-to-json-schema": "^3.25.2"
  }
}
